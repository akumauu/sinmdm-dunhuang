# Dunhuang Dance Motion Generation System
# 敦煌舞蹈动作生成系统

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
torch>=1.12.0
matplotlib>=3.5.0

# Optional: GPU acceleration
# torchvision>=0.13.0
# torchaudio>=0.12.0

# BVH processing (already included in our implementation)

# For SinMDM (will use its own environment.yml)
# Additional SinMDM dependencies are managed separately

# Development
pytest>=7.0.0
black>=22.0.0
